# On hold — rephotograph later

Moved here so `pair_orphan_slides.py` skips them until new block photos exist.

| File | Reason |
|------|--------|
| `slide_esophagus_MT_*.JPEG` | No matching block in library (or TWKO4 ambiguity resolved by removing old set_08) |
| `slide_lungs_MT_WT4_WO7842.JPEG` | No WT4 lungs block — rephoto |
| `slide_lungs_MT_WT5_WO7842.JPEG` | No WT5 lungs block — rephoto |

When ready: shoot block + barcode + slide, rename per `../README.md`, move back to `../incoming/`, run `python code/pair_orphan_slides.py --apply`.
