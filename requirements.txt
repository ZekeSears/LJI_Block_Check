opencv-python>=4.8
numpy>=1.24
# Pre-mortem §4 ¶2: hard-pin scipy>=1.12 even though EMD was dropped in
# the v2 constellation design — defense against future drift if EMD is
# reconsidered. phase3_constellation.py enforces this at import time.
scipy>=1.12
pandas>=2.0
matplotlib>=3.7
mahotas>=1.4
# Phase 3.5 — DataMatrix and QR decoding. System libraries (libdmtx,
# libzbar) are NOT installed by pip. Run code/phase35_setup_check.py
# for platform-specific install steps.
pylibdmtx>=0.1.10
pyzbar>=0.1.9
# Test runner.
pytest>=7.4
pytest-mock>=3.10
# Optional — only used by tests/test_phase35_id_parsing.py to generate
# synthetic QR fixtures.
qrcode>=7.4
